// import type { StrapiConfig } from "@strapi/types";

const config: Record<string, unknown> = {
  documentation: {
    enabled: true,
    config: {
      // basePath: '/docs', // optional kalau mau ganti path
    },
  },
};

export default config;
